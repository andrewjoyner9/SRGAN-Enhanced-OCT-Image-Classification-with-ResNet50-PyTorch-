This project explores the impact of super-resolution on medical image classification using Optical Coherence Tomography (OCT) data. A ResNet50 model is trained as a baseline on 128×128 images, then compared against a model trained on images enhanced using a Super-Resolution GAN (SRGAN). Performance is evaluated using accuracy, F1-score, and AUC. Implemented in PyTorch with GPU support.
